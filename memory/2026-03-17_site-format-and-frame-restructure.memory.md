## Site format update + Frame restructure (2026-03-17)

- **Top index (`index.html`)**: Reformatted navigation/sections, swapped subsystem order so **4 = Extraction & synchronisation** and **5 = Collection**, and fixed list markup.
- **Overview (`Overview/index.html`)**:
  - Contains the high-level context, subsystem legend table, assembled views slideshow, and **product flow** section.
  - No longer hosts the exploded/assembly/closeups slideshows (these were moved to Frame).
- **Frame (`Frame/index.html`)**:
  - Merged “How subassemblies fit together” content into the Frame top page under `#how-subassemblies-fit-together` (exploded view, assembly steps, colour legend, subsystem closeups, and narrative hierarchy/links).
  - Frame now links only to **Loads and load paths** as the remaining subsection.
- **Loads & load paths (`Frame/Loads-and-load-paths.html`)**: Now also hosts the Frame “Figures” slideshow (previously on Frame top).
- **Removed page**: `Frame/How-subassemblies-fit.html` deleted; inbound links/redirects updated to the new anchor on `Frame/index.html`.
- **Quality refresh**: Several subsystem gallery images and some Overview exploded images were replaced with higher-quality versions matched from `screenshots/`.

